# CHANGELOG

## 0.12.1（2026-08-13）— 年份与热力图联动交互调整

### 改进

- **默认与本周/本月/全部/自定义范围时：热力图显示「近一年」**（heatMode="recent"，不跟随范围）
- **"本年"chip 两段式点击**：第一次点击直接切换为"本年"范围（不弹选择器，热力图跟随当前年份全年）；再次点击（已是"本年"范围，含历史年份）→ 弹年份选择器
- **统计页年份选择器选项**：「近一年」+ 2023 ~ 当前年（当前年即"本年"高亮）；选具体年份 → 统计范围 = 该年全年 + 热力图同步；选「近一年」→ 仅热力图切近一年，统计范围 chip 保持"本年"
- **热力图自身切换**：选具体年份 → 回写统计页范围（rangeKey="year" + chip 显示该年）；切「近1年」→ 统计页范围不变
- sw.js CACHE_VERSION 同步递增 v0.12.1（两处同步）

## 0.12.0（2026-08-13）— 统计页年份选择 + 热力图联动；设置页统计图表管理

### 新增

- **统计页"本年"升级为年份选择**：
  - 点范围区"本年"chip → 弹出年份选择器（**2023 ~ 当前年**，复用 hy-picker 弹层）→ 选中年份 → chip 文案变"2023年"、统计范围 = 该年全年（1/1-12/31）
  - **热力图跟随年份**：默认按当前年（year-YYYY 模式全年 52 格按周聚合）；统计页年份变化 → 热力图同步切换；其他范围按钮（本周/本月/全部/自定义）不影响热力图
  - **双向同步**：热力图 hy-picker 选年份 → 回写统计页年份与范围（chip 同步显示）；选"近1年"仅热力图切换
- **设置页"统计图表"管理**（通用设置分组内，可展开/收起、接入 resetCollapsiblePanels）：
  - 7 个图表（每月次数/频率趋势/评分分布/日历热力图/星期分布/时段分布/标签排行）每行：名称 + 上移/下移按钮（首/末行对应方向禁用）+ 显示开关 checkbox（默认全开）
  - 存储：`preferences.chartOrder`（数组）与 `preferences.chartVisible`（对象）——savePref 持久化；loadConfig preferences 深合并（旧配置自动补默认值）
  - 统计页 renderCharts 按 chartOrder 重排 DOM + chartVisible 过滤渲染；单月自动隐藏柱状/折线的既有逻辑叠加保留
- **sw.js CACHE_VERSION 同步递增 v0.12.0**（发布约定：与 config.js APP_VERSION 两处同步）

## 0.11.1（2026-08-13）— Service Worker 更新机制修复

### 修复

- **线上更新不生效（用户看不到新版）**：sw.js 的缓存版本号原先动态 import 自 config.js——浏览器只在 sw.js 文件本身字节变化时才更新 Service Worker，而 import 语句字节不变 → SW 永不重新安装 → 旧缓存 cache-first 永远返回旧资源。修复：**CACHE_VERSION 在 sw.js 内硬编码**（发布时递增），每次发布 sw.js 字节变化 → 浏览器检测更新 → 新缓存安装 + 旧缓存清理
- **CORE_ASSETS 预缓存清单补齐**（新增 sync.js/export.js/stats.js/parse-import.js/sync-crypto.js/stats-page.js/settings-page.js/date-range-picker.js）
- **新版本提示**：SW 激活后向页面发送 SW_UPDATE_READY → Toast"发现新版本，请刷新页面后使用"

## 0.11.0（2026-08-12）— 图标替换为 Lucide 图标库

### 改进

- **20 个内联图标替换为 Lucide 标准线性图标**（内联、零 CDN，不引外部资源）：
  - 映射：calendar → calendar；star → star；starFill → star 闭合路径 + fill="currentColor" 实心（与 svg() 工厂 fill="none" 冲突通过工厂 fill 参数处理）；plus → plus；edit → pencil；trash → trash-2；search → search；filter → filter（标准漏斗）；tag → tag；settings → settings；chart → chart-column；export → download；import → upload；backup → database-backup；sync → refresh-cw；chevronLeft → chevron-left；chevronRight → chevron-right；close → x；check → check；empty → inbox
- **svg() 工厂适配 Lucide 标准**：stroke-width 1.8 → 2、viewBox 24、stroke-linecap/linejoin round、fill="none" 默认（starFill 传 "currentColor" 实心覆盖）；图标名称不变（引用无需改）

## 0.10.0（2026-08-12）— 数据导出上线

### 新增

- **数据管理分组实现"导出数据"**（展开式条目，与文本导入交互一致）：
  - **格式选择**：JSON / CSV / TXT（chip 单选，默认 JSON）
  - **筛选**：时间范围（起止 date 输入，默认全部）、评分（全部 / 1-5 星下拉）、标签（全部 / 具体标签下拉，动态填充现有标签）
  - **脱敏开关**：checkbox（"仅保留日期与评分，用于分享"）→ ExportModule.applyDesensitize
  - **导出按钮**：ExportModule.applyFilters 筛选 → toJSON / toCSV / toTXT → Blob 下载（CSV 前加 UTF-8 BOM `\uFEFF` 兼容 Excel；文件名 `dotoday_export_YYYYMMDD_HHMMSS.json/.csv/.txt`）→ Toast"已导出 N 条记录（格式，已脱敏）"；空数据 / 筛选结果为空 → 提示
  - 移除数据管理分组"导出…将在后续版本开放"占位文本
- **js/export.js**（新）：applyFilters（时间范围含端点 / 评分精确 / 标签任一命中）、applyDesensitize、toJSON（含 exportedAt/count）、toCSV（表头 + 字段转义，无 BOM 由 UI 加）、toTXT（每行完整字段）；契约，fix-1 并行实现可覆盖
- 导出面板接入 resetCollapsiblePanels 统一收起机制

## 0.9.0（2026-08-12）— 数据管理：本地备份 + 日志查看

### 新增

- **本地备份管理**（数据管理分组展开式条目，与文本导入交互一致）：
  - **手动创建备份**：「创建备份」按钮 → DataModule.createBackup(true)（强制创建，绕过当日去重）→ Toast 成功（备份名）
  - **备份列表**：DataModule.listBackups() → 每行（备份名 + 可读时间）+ 操作（恢复 / 导出 / 删除）；空态"暂无备份，点击'创建备份'手动备份"
  - **恢复**：选择备份 → DataModule.getBackup 预览（记录数 / 标签数 / 日期范围）→ 二次确认（"将用备份覆盖当前 N 条记录"）→ DataModule.restoreBackup 覆盖 → Toast + records-changed
  - **删除**：单个备份删除（DataModule.deleteBackup 新增：删除 backup store 指定名称）→ 二次确认 → 列表移除
  - **导出**：备份内容 → JSON Blob 导出（`dotoday_backup_时间戳.json`）
- **日志查看 / 清空**（展开式条目）：LoggerModule.getLogs() 最近 200 行等宽小字显示（可滚动、空态"暂无日志"）、「刷新」按钮；「清空日志」→ 轻确认 → LoggerModule.clearLogs()
- 数据管理分组：文本导入（已有）+ 本地备份 + 日志 + 导出占位；全部面板接入 resetCollapsiblePanels 统一收起机制
- DataModule 新增 getBackup（预览用，不覆盖主数据）/ deleteBackup 并导出

## 0.8.1（2026-08-12）— 同步分组细节调整

### 改进

- **加密私钥说明文案简化**：去掉算法细节（PBKDF2 / AES-GCM / 迭代次数），只写"用于 AES 加密"
- **GitHub 同步分组支持展开/收起**：与标签管理 / 文本导入一致的交互（分组标题行 + 「展开/收起」按钮，默认收起、标题常驻；展开时刷新最近上传时间与配置回填）；面板结构与标签管理对齐（.settings-group-head + .sync-panel 顶部浅虚线分隔）

## 0.8.0（2026-08-12）— GitHub 加密同步

### 新增

- **GitHub 同步分组（占位 → 正式；本地始终为权威数据源）**：
  - **配置项**：GitHub Token（password 掩码 + 获取指引链接 https://github.com/settings/tokens + repo 权限说明）、仓库（owner/repo）、加密私钥（password + 「记住私钥」checkbox——勾选明文存 localStorage `dotoday:secret` 并提示风险，未勾选每次输入、关闭页面即失）；token/repo 失焦自动保存配置，最近上传时间显示
  - **上传备份（日常）**：js/sync.js `uploadSync`——加密当前数据（records + tags + savedAt）→ PUT `current.json.enc`（固定名覆盖，带 sha 更新）+ `backup_时间戳.json.enc`（历史快照）→ 列出目录 → `planCleanup` 超 20 份通过 DELETE API 删最旧 → Toast 汇总（上传文件数 / 备份总数 / 清理数）；配置缺失（token/repo/私钥）逐一校验提示
  - **从 GitHub 恢复（灾难）**：`listRemote` 列出远端（current + 备份：名称/大小）→ 选文件 → `fetchRemote` 下载解密 → **预览**（记录数 / 标签数 / 日期范围 / 备份时间）→ 「确认覆盖」二次确认（"将用远端数据覆盖本地 N 条记录，不做合并"）→ DataModule.replaceAll 覆盖本地 → Toast + records-changed 联动刷新
  - **GitHub API 编排**：/repos/{owner}/{repo}/contents/dotoday-sync/ 目录；PUT/DELETE 带 sha；错误统一转清晰中文（401/403 → Token 无效或无 repo 权限、404 → 仓库不存在或目录为空、网络错误）；目录尚不存在时上传自动视为空
- **js/sync.js**（新）：uploadSync / listRemote / fetchRemote / splitRepo，动态加载 SyncCrypto
- **js/pure/sync-crypto.js**（新）：PBKDF2 310000 迭代派生 AES-256 密钥、AES-GCM 加密（随机 salt/iv 随密文存储）、encryptSnapshot / decryptSnapshot / buildBackupName / planCleanup（契约，fix-1 并行实现可覆盖）
- **DataModule.replaceAll**（新）：整体覆盖本地数据（恢复场景覆盖式，不做合并）

## 0.7.2（2026-08-12）— 标签配色恢复 7 色板 + 冲突自动变体

### 改进

- **改色选择器恢复 7 色板**（撤销 0.7.1 的 6 色系 × 4 档分组选择器）：简单圆点行（铅笔红 + 6 低饱和灰阶），当前色双圈高亮
- **颜色冲突系统自动微调（不要求用户选档）**：
  - config.js 新增 `resolveColor(base, usedColors)`：base 未被使用 → 原色；已被其他标签使用 → 沿 **HSL 明度阶梯**自动取第一个未用变体（明度 ±0.15 → ±0.3 → ±0.45 夹紧，再补饱和度 ±0.12 档），全部变体用尽才允许同色；hexToHsl / hslToHex / colorVariants 工具
  - **改色**：点 7 色之一被占用 → 自动采用该色未用变体，Toast"该色已被使用，已自动微调以区分"
  - **新建自动配色**（data.js pickTagColor 更新）：未用 7 基础色优先 → 全用尽后按基础色顺序自动变体（与 resolveColor 同逻辑）
  - 颜色仍存 hex，兼容现有数据；记录页/表单色点随现有机制联动
- 移除 TAG_COLOR_FAMILIES 色系结构（不再需要）；标签管理收起/展开（0.7.1）保留

## 0.7.1（2026-08-12）— 标签管理可收起展开 + 颜色色系模型

### 改进

- **标签管理分组可收起/展开**：分组标题行带展开按钮（与文本导入条目交互一致，点击切换"展开/收起"、默认收起、标题常驻）；展开时刷新标签列表
- **标签颜色改色系模型（6 系 × 4 档 = 24 色，同色系自动区分）**：
  - config.js 定义 `TAG_COLOR_FAMILIES`（红/橙/黄绿/蓝/紫/灰，每系 4 档 = 浅柔和 → 基准 → 加深 → 最深棕化，低饱和铅笔灰阶风两两可区分）；`DEFAULT_TAG_COLORS` 为其扁平展开（兼容既有引用）
  - **自动配色算法**（data.js `pickTagColor`）：最少使用色系优先 → 该系内未使用档位（按序）→ 24 色全用尽后轮转——同一色系多个标签自动落到不同档位，颜色不重复
  - **改色选择器**：色系分组显示（6 组 × 组标题 + 4 档圆点）；选中已被其他标签使用的档位 → **自动跳到该色系未使用档位**（Toast"该色已被使用，已为你选择本系其他档位"），同系标签始终不同档；该系全占才允许同色（极端场景）
  - 数据兼容：标签 color 仍存 hex 色值，无需迁移；记录页/表单色点随现有机制联动

## 0.7.0（2026-08-12）— 设置页标签管理

### 新增

- **标签管理分组（占位 → 正式，铅笔手绘风与全局一致）**：
  - **标签列表**：每行 = 色点（标签自带色）+ 名称 + 使用次数（统计自全部记录）+ 操作（重命名 / 改色 / 删除）；空态提示"还没有标签，输入上方新建"
  - **新增**：输入框 + 添加按钮（回车亦可）；DataModule.addTag 自动从默认色板配色（优先未使用色）；空名 / 重名 Toast 提示
  - **重命名**：行内编辑（回车 / 失焦提交、Esc 取消）→ DataModule.renameTag 同步更新所有记录的标签引用（Toast 显示受影响记录数）；重名提示
  - **改色**：色板弹层（复用 .color-dot 风格 + DEFAULT_TAG_COLORS 6 色）→ 新增 DataModule.changeTagColor（仅更新标签 color 字段，记录只存名称自动联动）；记录页 / 表单 / 统计页标签色点即时更新
  - **删除**：二次确认弹层（"删除标签「X」？将同步移除 N 条记录中的该标签"，复用 list.js 导出 showConfirm）→ DataModule.deleteTag 全局删除并同步移除记录引用 → Toast 显示受影响记录数
  - **联动**：任何操作后 bus.emit("records-changed") 刷新记录页 / 统计页标签排行；标签列表自身刷新（进设置页自动渲染）

## 0.6.2（2026-08-12）— 导入解析支持「年 + 中文月日」

### 修复

- **导入解析年份误判**（用户反馈：`2024年7月17日 (1)` 首行解析为坏行、其余行年份被错误推断为 2026）：parse-import.js 日期模式缺少「年 + 中文月日」——`2024年7月17日` 的 "2024年" 未被识别，部分行只匹配到 "7月18日" 并误用 refYear 推断年份。新增日期模式 `(\d{4})\s*年\s*(\d{1,2})\s*月\s*(\d{1,2})\s*[日号]`，**优先级高于省略年份的中文月日模式**（避免 "2024年7月17日" 被 "7月17日" 部分抢匹配导致年份丢失），支持 `2024年7月17日` / `2024年7月17号`（含次数 `(1)` 提取）；省略年份的上下文推断、原有全部格式（2024 1.1 / 2月2日 / 2024-01-01 / 1.1 等）保持原行为，新增 4 例单元测试

## 0.6.1（2026-08-11）— 折叠滚动空间修复

### 修复

- **数据少时 timeline 无法上滑折叠（回弹）**：内容高度 ≤ 容器高度时 maxScroll≈0，滚动驱动折叠无滚动空间（单条记录实测 maxScroll=13px）——`.timeline-inner` 加 `min-height: calc(100% + 200px)` 保证至少 COLLAPSE_RANGE 的折叠滚动空间；单条数据上滑可正常折叠（周历+timeline）、多条数据折叠/日期跟随/回顶展开回归正常

## 0.6.0（2026-08-11）— 设置页文本导入

### 新增

- **数据管理分组实现"文本导入"**（展开式交互，铅笔手绘风与全局一致）：
  - **输入区**：多行文本框（粘贴文本）+ 「文件导入」按钮（隐藏 input[type=file]，accept .txt/.json，读取文件内容填入文本框）+ 「开始解析」按钮 + 「清空」
  - **解析预览**：调用 ImportParser（js/pure/parse-import.js：normalizeText / parseText / expandRecords / validateItems 契约）解析 → 预览列表每条可编辑（日期 / 次数 / 备注输入框，可移除）；日期格式 2024-01-01 / 2024.1.1 / 2024年1月1日 / 1月2日 / 1.2，次数 (3) / ×3，备注任意文本
  - **坏行列表**：解析失败行原样列出（铅笔红标记 + 省略号），「修复」按钮把该行放回输入框修改后重新解析
  - **确认导入**：预览项（含修改）→ validateItems 复核 → expandRecords 展开（count>1 多条）→ DataModule.addRecord 逐条写入（评分默认 null、tags 空数组；date+note 相同记录合并跳过）→ Toast「已导入 N 条，跳过 M 条」→ bus.emit("records-changed") 刷新记录页 / 统计页
  - 解析器动态加载防御（未就绪时降级提示）；「重置」清空全部状态
- 数据管理分组导出 / 备份 / 日志保持占位

## 0.5.2（2026-08-11）— "热力图主色"升级为"图表配色"

### 改进

- **设置项"热力图主色"升级为"图表配色"**：文案与 hint 更新（"统计页图表的深浅配色"）；配置 key 保留 `preferences.heatmapColor`（已选配置不丢）
- **选中色生成 5 档色阶**（--chart-1..5）：1 = 主色 0.15 透明度、2 = 0.35 透明度、3 = 主色实色、4 = 主色加深 15%、5 = 主色加深 30%（hexToRgb / rgbA / darkenRgb / lightenRgb 工具扩展）；默认铅笔红 #9c5236；深色模式下主色自动提亮（默认用 #c98b6d 与 --accent 一致，自定义色提亮 12%），监听 prefers-color-scheme 变化重建色阶
- **统计页全部图表改用色系**：评分分布堆叠条 5 段 = --chart-1..5（1 星最浅 → 5 星最深）；时段分布 4 段 = --chart-1/2/3（"未记录"段保留灰阶 --text-muted 语义区分）；柱状图 / 折线图 / 星期分布 = --chart-3 主色；日历热力图 4 级与图例 = --chart-1/2/3（0.5.1 的 --heat-* 机制统一迁移到 --chart-*，删除重复变量）；标签排行保留标签自带颜色（不随配色）

## 0.5.1（2026-08-11）— 设置页视觉修复

### 修复

- **热力图主色色板圆点不显色**：HEAT_PALETTE 仅首项有 color 且渲染未写背景 → 7 个圆点全为透明浅灰；已补齐全部色值（灰棕 / 灰蓝 / 灰绿 / 灰紫 / 暖灰 / 蓝灰）并在圆点渲染加 `style="background:…"`，默认铅笔红亦显示 #9c5236
- **热力图图例不跟随主色**：.heat-legend .heat-lv.h1/h2/h3 硬编码 --accent-* → 改为 `var(--heat-1/2/3, --accent-*)`，选主色后图例 4 色块与格子同步变化
- **列表排序行窄屏文字竖排**（根因：.settings-row-main flex: 0 1 auto + min-width: 0，排序行 options 4 chips 占满宽度把左列挤压至 ~0px → "列表排序"竖排 4 行、hint 竖排 10 行）：① .settings-row-main 改 `flex: 0 0 auto` 绝不收缩；② 排序行与色板行改上下结构（.settings-row-stack：标签上方整行、chips / 色板下方整行左对齐），任何宽度下整齐不散乱；周起始行保持左右两列

## 0.5.0（2026-08-11）— 设置页上线

### 新增

- **设置页（5 个分组卡片 + 独立滚动容器，铅笔手绘风与记录/统计页一致）**：
  - **通用设置（完整实现，即时生效）**：
    - 周起始日：周一 / 周日 chip 二选一（默认周一），保存 `preferences.weekStartDay`，变更即重渲染日历 / 单周条 / 统计按周范围
    - 列表排序：时间倒序（默认）/ 时间正序 / 评分排序（评分高在前、无评分最后）/ 标签排序（按首个标签名）chip 单选，保存 `preferences.defaultSortOrder`，变更即重渲染列表
    - 热力图主色：色板（默认铅笔红 + 6 个低饱和灰阶选项，圆点点击），保存 `preferences.heatmapColor`；统计页热力图 4 级深浅由主色生成——浅格 / 中格为主色 0.15 / 0.4 透明度、深格主色加深 20%，生成后写入 `--heat-1/2/3` CSS 变量（默认回退 --accent-*，明暗两套自动适配）
  - **标签管理 / 数据管理 / GitHub 同步**：框架占位卡片（后续版本开放）
  - **关于**：版本号（config APP_VERSION 驱动）+ 开发者 Hau
- 配置联动：settings-page.js 保存配置后 emit `config-changed`，app.js 编排重渲染日历 / 单周条 / 列表；设置页渲染（选中态 / 色板 / 版本）挂接 tab-changed
- 新增 js/ui/settings-page.js：色板渲染、chip 交互、主色 → 深浅档生成（hexToRgb / rgbA / darken）、配置读写（getPref / saveConfig）

## 0.4.8（2026-08-11）— 时段堆叠条 + 标签排行重构

### 改进

- **时段分布改为水平堆叠条**：由 4 行独立横向条形改为一条 100% 堆叠条（早/午/晚/未记录 4 段，段宽按占比，0 次段为最小可见淡色细分隔）；颜色 4 档——早/午/晚复用铅笔红深浅（--accent-soft / --accent-mid / --accent-deep），"未记录"用灰阶（--text-muted 低透明度）区分；段点击 → tooltip（时段名 · 次数 · 占比）；下方一行图例（色块 + 时段名 + 次数小字）；与评分分布共用堆叠条组件（.stack-bar/.stack-seg/.stack-legend 参数化，新增 gray 档）
- **标签排行重构为行式布局**：每行 = 色点 + 标签名（固定宽度列，按最长标签自适应、上限 140px、超长省略号 title 完整可读）+ 次数/占比数字 + 独立比例条（占比、标签色）；标签名不再与条形挤压；色点与比例条引用**标签自带颜色**（与记录页同模式 DataModule.getTags().find(name).color，标签已删除时回退铅笔红 --accent）；行点击 → tooltip（标签名 · 次数 · 占比）保留；行高 28px 紧凑，全部标签直接显示

## 0.4.7（2026-08-11）— 统计页细节修正

### 修复

- **去掉评分分布"共 N 条评分"文字**：水平堆叠条上方保持干净
- **热力图格内月份数字颜色调淡**：浅格用 --text-muted + opacity 0.6（柔和的淡灰字）、深格 .on-dark 用纸色 + opacity 0.75（降低对比，保持可读但柔和）
- **热力图最新一格修正为本周**：原实现把本周日当作最后一周的起始日导致显示到下周；现计算"本周起始日"（跟随应用配置 weekStartDay，monday/sunday 与日历一致）并以其往前 51 周为起点，第 52 格 = 本周（起始日 +6 天）；年份模式同样按配置对齐周起始；周聚合 / tooltip 周起止 / 格内月份数字同步适配
- **修复 0.4.7 引入的统计页崩溃**：renderHeatmap 内 `const now` 重复声明导致模块加载失败、整个应用空白；已移除重复声明（回归：记录页 / 统计页渲染均正常）

## 0.4.6（2026-08-11）— 评分分布堆叠条 + 热力图方向与月份标注

### 新增

- **评分分布重构为水平堆叠条**：100% 宽横条分 5 段（1-5 星，段宽按占比，0 次段最小可见淡色细分隔），5 档铅笔红深浅渐变；条上方右侧"共 N 条评分"；下方一行图例（色块 + N星 + 次数小字）；点击段或图例项 → tooltip（星级/次数/比例）；移除环形图渲染（.donut-wrap/.donut-seg 等全部清理）
- **热力图方向修正为按列填充**：按列从上到下为旧到新、按行从左到右为旧到新（第 1 列 = 第 1-5 周 … 第 11 列 = 第 51-52 周 + 3 占位格）
- **格内月份数字标注**：移除底部月份标签；每格周区间包含某月 1 号时在格内显示该月数字（10px 加粗居中），对比色自动取反（深格浅字 / 浅格深字）

## 0.4.5（2026-08-11）— 热力图布局与配色修复 + 环形图视觉平衡

### 修复

- **热力图全黑**：rect 模板 `h${lvl}` 与 lvl（已是 "h0" 前缀）拼成 "hh0"，CSS `.heat.h0-h3` 不匹配导致默认黑色——改为 `heat ${lvl}`，4 级铅笔红深浅恢复
- **热力图改 11 列 × 5 行**：52 格按行填充（第 1 行第 1-11 周 … 第 5 行第 45-52 周 + 3 淡色占位格保持网格规整）；格子尺寸按容器可用宽计算（24px + 4px gap），网格整体居中（左右留白对称）、完全显示无溢出；底部月份标注随列布局
- **环形图视觉平衡**：图例列加宽（min-width 76px）并补次数信息（色块 + "N星" + "N次"，次数弱化色小字），环+图例组合整体居中（justify-content: center），视觉重心对齐容器中心

## 0.4.4（2026-08-11）— 环形图左右布局 + 热力图每周一格改版

### 新增

- **环形图改为左环右图例**：左侧圆环 + 右侧竖列 5 项图例（色块 + 星级，无数据星级淡色块，紧凑 26px 行高），环直径适配（170px）；点击扇区或图例项 → tooltip（星级/次数/比例）；中心总评分数保留
- **热力图 52 格每周一格**：固定 52 个格子（13 列 × 4 行，从左到右从上到下，第 1 格 = 最早一周、第 52 格 = 最近一周），每格 = 该周 7 天记录合计；格子 22px 清晰可点、0.8px 淡描边；4 级深浅（0/1/2-3/4+）；底部月份标注（统一"2月"格式）；点击格子 → tooltip 显示周起止日期与次数（"2026年6月8日 - 6月14日 · 3 次"）；年份选择保留（近1年 52 周 / 选某年该年 1/1 所在周起 52 周）；移除周几标签

## 0.4.3（2026-08-11）— 统计页交互重构

### 新增

- **单月范围隐藏按月图**：时间范围为单月（本月 / 自定义同月）时隐藏"每月次数"与"频率趋势"两张图；跨月范围正常显示
- **环形图一行图例**：图例改为一行横排 5 项（色块 + 星级，无数据星级淡色块），紧凑贴图；中心总评分数保留
- **热力图改近 1 年按周**：默认近 1 年（今天往前 52-53 个自然周，格宽自动适配）；右上角 chip 点击弹出年份选择器（近1年 / 当前年及前 3 年），选某年显示该年全年热力图（chip 显示年份）
- **统一 tooltip 组件**：全部图表点击改为图中铅笔手绘风 tooltip（虚线边框/纸色底/手绘阴影/防溢出），移除底部详情卡（#stats-detail 已删除）——柱/折线显示月份次数、环形显示星级/次数/比例、标签显示次数/占比、热力图格子显示日期/次数

## 0.4.2（2026-08-11）— 统计页视觉修订

### 修复

- **统计页底部被 tab 栏遮挡**：滚动容器内容层加底部 padding 补偿（.stats-scroll::after = tabbar 高度，桌面 90px），滚到底最后内容完整可见
- **热力图**：高度固定为"顶部留白 + 7 行格子 + 月标签"（消除下方大片留空）；4 级铅笔红深浅层次（无记录淡纸色 / 1 次浅 / 2-3 次中 / 4 次以上深红棕，新增 --accent-deep），格子细描边，新增"少→多"极简图例
- **环形图**：配色改 accent 系 5 档由浅到深（新增 --accent-deep/--accent-deeper，避免暗沉）；中心加纸色留白圆（总评分数字与扇区不重叠）；0 次星级段淡色虚线描边占位；新增图例行（色块 + 星级 + 次数 + 百分比，点击可切详情卡）
- **柱状图/折线图**：绘图区左右留白对称（内框水平居中）；移除无意义"max"文字；新增 y 轴刻度（0/中值/最大值 + 淡色虚线水平网格线）
- **自定义范围弹层可用性**：起止改"开始日期：X / 结束日期：Y"可点击按钮（激活铅笔红描边）；新增操作提示条（"点击日历选择开始日期"→ 自动切"选择结束日期"）；选择开始后自动进入结束选择；区间中间日期 accent-soft 高亮连接

## 0.4.1（2026-08-11）— Tab 切换重大修复

### 修复

- **切换统计/设置页仍显示记录页**（P1）：`#page-record { display: flex }` 独立 ID 选择器优先级高于 `.page` 的 `display: none`，覆盖了 Tab 显隐机制（0.3.0 独立容器重构引入）——记录页永远可见，切到统计时两页同时显示。修复：`display: flex` 移到 `#page-record.active` 下，非激活时 `.page` 隐藏规则生效

## 0.4.0（2026-08-11）— 统计页上线

### 新增

- **统计页**（原占位 → 正式）：概览卡片 4 张（总次数/环比/距上次天数/平均评分，全部跟随所选时间范围）
- **图表区 7 个**（手写 SVG + 铅笔手绘风）：柱状图（按月）、折线图（按月趋势）、环形图（评分分布，只统计有评分）、日历热力图（固定不跟随主范围，自身可切换近 3/6/12 个月）、星期分布、时段分布（早/午/晚/未记录）、标签排行（次数降序横向条形）
- **时间范围**：快捷项（本周/本月/本年/全部，默认本月）+ 自定义日期范围选择器（起止两日期、日历风格弹层、未来禁用）
- **图表交互**：点击数据点 → 图表下方详情卡（柱/折线/环形/标签排行）
- 统计页独立容器滚动（与记录页一致）；图表自动压缩适配宽度（无横向滚动）
- **stats.js 纯逻辑模块**（8 个统计函数，node 可测）+ 单元测试 19 例

### 说明

- 热力图主色暂用主题点缀色（设置页自定义主色待设置页版本实现）

## 0.3.2（2026-08-11）— 折叠动画速度恢复

### 修改

- 折叠动画速度恢复原值（0.3.1 放慢 3 倍后用户反馈"太奇怪"）：SCROLL.COLLAPSE_RANGE 600 → **200**、指数趋近系数 0.1 → **0.3**
- 底部 padding 补偿（0.3.1 最旧记录遮挡修复）保留

## 0.3.1（2026-08-11）— 底部遮挡修复 + 折叠动画放慢

### 修复

- **最旧数据被底部 tab 栏遮挡**：timeline 容器底部 padding 补偿 = tabbar 高度（`calc(72px + safe-area)`，桌面 90px）——滚动到底时最后一条记录能完整滑出到 tab 栏上方可见
- **折叠动画放慢约 3 倍**：SCROLL.COLLAPSE_RANGE 200 → **600**（滚动 600px 才完成折叠，跟手更慢更有质感）；指数趋近系数 0.3 → **0.1**（滚动停止后的收敛动画慢 3 倍）；点击定位的强制折叠（同步 progress=1）不受放慢影响，仍即时折叠

## 0.3.0（2026-08-11）— 独立容器滚动模型（主界面重构）

### 新增

- **独立容器滚动**：页面固定视口高度不再滚动（body overflow hidden），滚动只发生在 timeline 独立容器内（flex 列：标题 → 日历容器（高度可变）→ timeline 容器 flex:1 + overflow-y:auto）
- **上滑交互 = 此消彼长**：日历容器高度随 --collapse-progress 插值（399px → 64px 单周条），timeline 容器高度 flex 自适应增大
- **先折叠、后滚动**：timeline 容器 scrollTop 0-200px 阶段全部用于日历折叠——内容层（.timeline-inner）以 transform 偏移抵消滚动，内容视觉不动；折叠完成后内容开始正常滚动；最新记录随容器顶部上移，折叠完成时停在预留空间底部（标题 84 + 单周条 64 + 间距 8）
- **单周条在预留空间内分段淡入**（progress 0.7→1，无重叠）；慢速滚动停在折叠完成态（最新记录在容器顶部），快速滚动自然滚过内容
- **点击日历定位适配容器**：同步强制折叠 → 目标记录 offsetTop → 容器 scrollTop = T + COLLAPSE_RANGE（记录位于容器顶部 +8px）；最旧记录 maxScroll 限位；滚动跟随/保护窗口/月份浮层全部改为容器坐标

### 说明

- 移除页面整体滚动（window scroll）驱动；header 恢复为 flex 项（页面不滚动自然固定）

## 0.2.7（回退，2026-08-11）— 撤销 0.3.0/0.3.1 吸顶预留空间模型

### 修复

- **撤销吸顶预留空间模型**（方向错误，待重新设计）：移除 0.3.x 的 sticky 吸顶（.sticky-target / uiState.stickyDate / applyStickyTarget）、动态吸顶位、纸色描边；`.tl-item` 恢复普通文档流
- **scrollToDate 恢复 0.2.7 语义**：① 同步强制折叠（布局稳定）→ ② 计算目标 = 该日第一条记录文档位置 - (标题 + 单周条 + 8px) → ③ 滚动到 min(target, maxScroll)（最旧记录限位回弹）
- **保留**：滚动驱动折叠动画（--collapse-progress 连续插值、可中断可暂停、分段淡入淡出防重影）、点击定位强制折叠 + 数据少时可见、0.3.1 回顶展开修复（y≤1 取消 forcedCollapse 恢复滚动驱动）

### 说明

- 0.3.0 / 0.3.1 历史条目保留供追溯

## 0.3.1（2026-08-11）— 回顶展开修复

### 修复

- **回顶后日历不展开**（0.3.0 回归）：点击定位触发的强制折叠（forcedCollapse）只在用户触摸/滚轮时取消，程序化回顶（scrollY=0）只重置了吸顶目标未取消强制折叠 → 日历保持折叠。修复：回顶（y≤1）时同步取消 forcedCollapse，恢复滚动驱动展开

## 0.3.0（2026-08-11）— 吸顶预留空间模型

### 变更

- 主界面交互重构为**吸顶预留空间模型**：timeline 顶部（最新记录）滚动到预留空间底部后吸顶钉住（最高不超过预留空间底部，永不进入、永不被遮挡）；周日历（单周条）在预留空间内滚动驱动缓缓淡入，与 timeline 零重叠
- 点击日历某日期 → 该日第一条记录成为吸顶目标（sticky 切换）+ 滚动到位；回顶恢复默认最新一条并展开完整月历
- 移除 0.2.8 的折叠锁定（collapseLocked）与吸附（snapToNewest）逻辑（主界面交互重构）

### 新增

- **吸顶预留空间模型**：标题（fixed）下方、单周条区域（约 84-148px）为预留空间；单周条随滚动进度在空间内分段淡入（0.7→1），**timeline 内容永不进入预留空间**
- **timeline 吸顶**：折叠态最新一条记录（默认）或点击日期后的该日第一条记录 `position: sticky`，钉在预留空间底部（top 随折叠进度动态下移 = 标题 + 单周条动态高度 + 6px）——上滑时目标记录滚到吸顶位后钉住不再上移，旧记录从下方继续滚动；吸顶卡片加纸色描边遮挡穿过内容
- **点击日期 → 吸顶目标切换**：点击日历某天，该日第一条记录成为吸顶目标并滚动到吸顶位（至少到折叠完成位，单周条全显）；滚动浏览时吸顶目标保持，回顶（scrollY=0）恢复默认最新一条并展开完整月历

### 修复

- **移除 0.2.8 的折叠锁定 + 吸附逻辑**：吸顶替代吸附（上滑折叠完成后最新记录自然钉在吸顶位，无需回弹滚动）；吸顶位动态跟随折叠进度，单周条淡入占据空间时记录被"推"下，永不重叠

## 0.2.8（2026-08-11）— 折叠锁定 + 最新记录吸附

### 修复

- **上滑后最新记录被顶部区遮挡**：折叠完成后（progress=1）折叠状态**锁定**，检测最新记录被标题+单周条遮挡时平滑吸附到"该记录紧贴单周条下方 8px"（数据不足/记录本就可见时不吸附）；锁定期间 progress 保持 1（吸附后 scrollY 低于折叠阈值也不回退展开），继续上滑浏览旧记录正常；滚动回顶（scrollY=0）解锁、恢复滚动驱动展开
- **点击定位后锁定**：scrollToDate 强制折叠同时锁定折叠态，定位滚动中 progress 不回退（避免偏移），回顶解锁

## 0.2.7（2026-08-11）— 详情标签去重 + 折叠过渡动画优化

### 修复

- **展开详情不再重复显示标签**：标签已在卡片主行默认显示（.tl-tags-inline），展开详情（.tl-detail）移除标签区只保留备注；无备注时无可展开内容
- **折叠过渡动画错乱**：月历与单周条淡入淡出改为**分段错开**——月历内容在 progress 0→0.3 期间快速淡出（整体透明后容器高度继续插值收缩，不再逐行截断压扁）；单周条在 progress 0.7→1 期间才淡入；任意中间态只有一层半透明，消除日期重影、遮罩条、两层叠加；展开方向对称

## 0.2.6（2026-08-11）— 点击定位彻底重构

### 修复

- **点击日历日期 → timeline 定位彻底理顺**（三步时序）：① 先强制完整折叠（同步收敛布局，折叠后布局固定为"标题 fixed + 单周条 sticky"，不再随滚动位置变化）；② 布局稳定后计算目标 = 该日最新一条记录紧贴单周条下方 8px；③ 滚动到 min(target, maxScroll)——目标可达精确滚动，最旧记录区域由 maxScroll 限位（列表底部自然停住/原生回弹，不越界）
- 修复滚动距离不足以完全折叠（目标 scrollY 在 0-200px 间）时半折叠布局与目标计算不一致导致的定位偏差；移除冗余的 COLLAPSE_AT 预判逻辑

### 说明

- 点击后日历保持折叠（滚动回顶自然展开，原有机制不变）；保护窗口与滚动跟随逻辑保持

## 0.2.5（2026-08-11）— 标题彻底固定 + 滚动驱动折叠动画 + 点近日期定位修复

### 修复

- **顶部标题彻底固定**：.app-header 由 sticky 改为 fixed（脱离滚动流，任何滚动位置都在视口顶部），记录页内容区以 padding-top（--header-h）占位；折叠单周条仍与标题对齐（top: var(--header-h)）
- **点近日期定位失败**（数据量少时）：scrollToDate 判断目标滚动位置超过 maxScroll（不可达）时触发强制折叠（bus "force-collapse"）——折叠后日历区收起、目标记录上移到单周条下方可见；用户滚动/触摸/鼠标交互后恢复滚动驱动

### 优化

- **日历折叠改为滚动驱动连续动画**：scrollY 0→200px（SCROLL.COLLAPSE_RANGE）连续映射折叠进度 0→1，写入 --collapse-progress CSS 变量；日历高度/透明度、单周条淡入、添加按钮收起全部用 calc 插值；进度以指数趋近方式平滑（每帧 30%），滚动可中断、可暂停（中间停住保持半折叠）；折叠 class 判定用进度 0.6/0.4 hysteresis；移除固定时长 transition

## 0.2.4（2026-08-11）— 补位格切月修复

### 修复

- **完整日历点击补位格（跨月日期）不切月**：完整日历点击路径统一改用 `setCalendarDate()`（原实现只更新选中并重渲染当前月，点击上月/下月补位格时月标签不切换、选中停留在补位格上）；与折叠单周条路径行为一致，跨月点选自动切月渲染

## 0.2.3（2026-08-11）— 点选保护窗口修复

### 修复

- **用户点选日期后选中被滚动跟随覆盖**（数据量不足时必现）：点选日期（完整日历 / 折叠单周条 / 今天按钮）时记录 1 秒保护窗口（uiState.pickGuardUntil，SCROLL.PICK_GUARD_MS=1000），滚动跟随在保护窗口内跳过更新选中——修复数据量少、内容高度不足以滚动到目标日时，scrollToDate 被 maxScroll 钳制、跟随逻辑把选中改回可视区顶部日期导致点选无效的问题；保护窗口过期后跟随恢复正常（此时定位滚动已结束，可视区顶部与点选日期一致）

## 0.2.2（2026-08-11）— 日期联动细节修复

### 修复

- **添加记录默认日期 = 日历当前选中日期**：新建记录时表单日期默认取首页日历选中日期（uiState.selectedDate，含折叠单周条点选更新的场景），未选中时才回退今天；编辑记录仍回填原日期
- **点日历定位被单周条遮挡**：scrollToDate 预判目标滚动位置是否超过折叠阈值——超过则日历必折叠、单周条 sticky 占位，顶部偏移叠加单周条高度（读 CSS maxHeight），目标记录紧贴单周条下方可见；未超过阈值仍只减标题高度
- **折叠单周条日期点击支持**：单周条 7 个日期格绑定点击，与完整日历一致（更新选中、跨月切月、同步单周条、触发列表定位）

## 0.2.1（2026-08-11）— 滚动体验修复 + 标签默认显示 + 实心星修复

### 修复

- **折叠单周条固定可见**：滚动折叠后单周条 sticky 固定在标题下方（top 由 JS 按实际标题高度注入 --header-h），不透明背景，列表滚动穿过时不透；下滑回顶恢复完整月历
- **日期跟随节流**：同月内日期切换最小间隔 250ms（SCROLL.FOLLOW_MS），快速滑动不疯狂跳变；跨月仍即时切月重渲染；选中态背景/边框加 0.18s 平滑过渡
- **实心星修复**：icon-config.js 的 svg() 模板改为 fill 参数（fill="none" 空心描边 / "currentColor" 实心填充），修复 HTML 重复属性导致 starFill 恒为空心的 bug——列表与表单中选中星级为实心（铅笔红填充）、未选中为空心描边

### 新增

- **列表默认显示标签**：有标签的记录卡片主行下方直接显示小号标签 chip（tag-dot 色点 + 名称，铅笔手绘风），无标签不占空间（卡片保持 41px 紧凑），展开详情保留原标签展示

## 0.2.0（2026-08-11）— 主界面交互大升级（全量滚动 + 日历联动）

### 新增

- **Timeline 全量滚动**：列表默认渲染全部记录（时间倒序，最新在最上），可一直向上滑动跨日期浏览，不再局限于选中日期；按月分组标题保留
- **日历折叠联动**：滚动超过阈值（80px，rAF 节流）→ 日历从完整月历折叠为单周条（仅显示选中日期所在一周 7 天，周起始规则一致，今天铅笔红/选中实底/记录小圆点），添加记录按钮同步隐藏；下滑回滚（低于 50px，hysteresis）→ 完整月历与按钮恢复，带高度/透明度过渡动画
- **标题固定**：滚动时顶部 DoToday 标题始终固定可见（sticky）
- **日期跟随**：滚动中实时以可视区顶部记录日期更新日历选中态，跨月自动切换月份
- **日历点选定位**：点击展开态日历某天 → 列表平滑滚动到该日期第一条记录（保持全量视图）；今天按钮同步定位；reduced-motion 时直接跳转

### 修复

- 记录卡片高度：内边距 10px → 7px，卡片高度由内容决定——无备注/无标签/无评分时紧凑单行（约 38px），不再出现大面积空白；展开详情上限 260px → 480px，长备注自然增高

### 说明

- sw.js 的 CACHE_NAME 由 config.js APP_VERSION 自动派生为 dotoday-0.2.0

## 0.1.2（2026-08-11）— P1 修复 + 视觉优化

### 修复

- **P1：添加/编辑记录保存后列表不立即显示**——list.js 未订阅 "records-changed" 事件（仅日历订阅了），保存后列表保持旧状态；现在 list.js 订阅该事件，保存后列表即时刷新显示新记录，日历记录标记同步更新
- 表单标题区不固定：sheet 改为 flex 三段式布局（固定头部：拖动条 + 标题 → 滚动内容区 → 固定底部：取消/保存按钮），内容滚动时标题与操作按钮始终可见；保存按钮通过 form 属性关联表单提交

### 优化

- 左滑操作按钮与记录卡片增加 8px 间距（按钮组 padding-left + 滑开位移同步调整），视觉上有呼吸空间
- 线条规则重调（全部虚线）：外层容器边框用深色虚线（2px dashed var(--line)），内部元素（输入框/快捷按钮/标签输入/时间框/滚轮分隔/字段分隔线/Tab 栏等）改用浅色虚线（1px dashed var(--line-soft)，浅色 #c9c2b2 / 深色 #4a463d）；移除不再使用的 --line-faint 变量

### 说明

- 版本机制：sw.js 的 CACHE_NAME 已统一由 config.js 的 APP_VERSION 派生（"dotoday-0.1.2"），无需单独改 sw.js

## 0.1.1（2026-08-11）— 缩放禁止 + 线条规则统一 + 代码结构优化

### 优化

- 禁止双指/双击缩放：viewport 增加 `maximum-scale=1.0, user-scalable=no`，`html, body` 增加 `touch-action: manipulation`（同时消除双击缩放延迟），更接近原生 App 体验
- 线条规则统一（plan 0.3.1 规范）：虚线（2px dashed）仅用于主轮廓（sheet 顶线、日期显示框、记录卡片、日历容器、空态框、弹层容器，同屏 ≤ 3 处）；内部结构（字段分隔、输入框、按钮/chip、滚轮分隔、Tab 栏、工具栏、浮层提示）全部改为淡实线（1px solid var(--line-faint)）；时间轴竖线与记录日虚线圈保留为装饰性弱化虚线

### 代码结构

- 新增 js/config.js：集中全部应用常量（APP_VERSION / STORAGE_KEYS / DB / DEFAULT_TAG_COLORS / LIMITS / TOAST_MS / CACHE_NAME / 排序方式 / 默认配置）与配置读写（loadConfig / saveConfig / getPref），消除散落魔法数字（备注 500 字、标签名 12 字、撤销 5 秒、备份 10 份、日志 200KB 等）
- app.js 模块拆分（约 1250 行 → 入口约 130 行）：新增 js/ui/ 组件目录
  - common.js（DOM 工具 + 轻量事件总线）、state.js（跨模块 UI 状态）
  - calendar.js（主日历 + 年月选择器）、list.js（Timeline + 删除撤销 + 批量 + 月份浮层）、form.js（表单 + 校验 + 保存）、date-picker.js（日期选择面板）、time-wheel.js（时间滚轮）、toast.js、tabs.js
  - 模块间通过轻量事件总线通信（records-changed / calendar-changed），数据以 DataModule 为唯一事实源
- data.js / logger.js / sw.js 改为从 config.js import 常量；sw.js 升级为 module 类型 Service Worker（import CACHE_NAME），注册失败静默降级

### 说明

- 功能与视觉保持 0.1.0 完全一致（纯重构 + 规范统一）

## 0.1.0（2026-08-11）— 基础框架 + PWA 壳 + 记录页核心

### 新增

- 项目文件结构：index.html / manifest.json / sw.js / css / js（ES Modules 模块化）
- 铅笔手绘风主题（sketch-style）：明/暗两套配色跟随系统（prefers-color-scheme），token 以 CSS 变量注入
- 记录页核心：日历组件（月切换、年月选择器、今天按钮、记录日深浅标记）+ Timeline 列表（左侧时间轴、按月分组、点击展开详情、左滑编辑/删除、删除 5 秒撤销、批量删除、月份浮层、空态、骨架屏）
- 添加/编辑表单：底部弹出、日期快捷（今天/昨天/前天）、滚轮时间选择器（可清除）、评分星星（可清除）、标签多选与新建、备注 500 字限制、仅日期必填、禁止未来日期、编辑回填
- 数据层（IndexedDB）：记录 CRUD、每日自动备份（保留 10 份）、标签管理（自动配色/重命名/全局删除）、搜索、筛选、失败重试 3 次
- 纯逻辑模块 date-utils（日期格式化/解析/时段推断/日历网格）+ 单元测试 20 例（node --test 全部通过）
- 日志模块（error.log，200KB 轮转保留 1 份 .old）
- PWA：manifest（DoToday、standalone、双尺寸图标）、Service Worker 离线缓存（cache-first + 版本清理）
- 应用图标：铅笔手绘风 icon-192 / icon-512（全幅铺满正方形）

### 说明

- 统计页、设置页为占位页（后续版本实现）
- 数据存储于浏览器 IndexedDB（本地明文，云端加密同步在 v0.5.0 提供）
- 本地预览固定端口 **8642**（Tailscale 远程预览：http://100.96.220.54:8642），后续开发沿用
